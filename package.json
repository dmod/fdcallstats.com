{
  "name": "fdcallstats",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "8.9.1"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "express": "^4.15.2"
  },
  "devDependencies": {},
  "license": "MIT"
}
